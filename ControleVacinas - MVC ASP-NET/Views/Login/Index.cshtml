@model LoginModel
@{
    Layout = "_layoutDeslogado";
    ViewData["Title"] = "Login";
}

<div class="Text-center">
    @if (TempData["MensagemErro"] != null)
    {
        <div id="alert-box" class="alert alert-danger custom-alert" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" arial-label="Close">x</button>
            @TempData["MensagemErro"]
        </div>
    }

    @if (TempData["MensagemSucesso"] != null)
    {
        <div id="alert-box" class="alert alert-success custom-alert" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" arial-label="Close">x</button>
            @TempData["MensagemSucesso"]
        </div>
    }
</div>

@*     <div class="title-container">
        <div>SCVC | Sistema de Cadastro de Vacinação dos Servidores</div>        
    </div> *@

@* <div class="login-container">

        <form asp-controller="Login" asp-action="Entrar" method="post">
            <div class="mb-3">
                <label for="login">Login</label>
                <input type="text" asp-for="Login" class="form-control" id="login" placeholder="Digite seu login">        
                @Html.ValidationMessageFor(x => x.Login)
            </div>
            <div class="mb-3">
                <label for="senha">Senha</label>
                <input type="password" asp-for="Senha" class="form-control" id="senha" placeholder="Digite sua senha">
                @Html.ValidationMessageFor(x => x.Senha)
            </div>

@*             <p class="mb-4 text-sm mx-auto">
                Esqueceu sua senha ?
                <a asp-action="RedefinirSenha" asp-controller="Login" class="text-primary text-gradient font-weight-bold">Redefinir minha senha</a>
            </p>
 *@
 @*            <button type="submit" class="btn btn-primary btn-block">Acessar</button>
        </form> *@

    @* </div>  *@


<div class="center-wrapper">

    <div class="title-container">
        <div>SCVC | Sistema de Cadastro de Vacinação dos Servidores</div>
    </div>

    <div class="login-container">

        <form asp-controller="Login" asp-action="Entrar" method="post">
            <div class="mb-3">
                <label for="login">Login</label>
                <input type="text" asp-for="Login" class="form-control" id="login" placeholder="Digite seu login">
                @Html.ValidationMessageFor(x => x.Login)
            </div>
            <div class="mb-3">
                <label for="senha">Senha</label>
                <input type="password" asp-for="Senha" class="form-control" id="senha" placeholder="Digite sua senha">
                @Html.ValidationMessageFor(x => x.Senha)
            </div>

            @*             <p class="mb-4 text-sm mx-auto">
            Esqueceu sua senha ?
            <a asp-action="RedefinirSenha" asp-controller="Login" class="text-primary text-gradient font-weight-bold">Redefinir minha senha</a>
            </p>
            *@
            <button type="submit" class="btn btn-primary btn-block">Acessar</button>
        </form>

    </div>
</div>

